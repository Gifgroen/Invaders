{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile invaders (MacOS)",
            "type": "shell",
            "command": "sh",
            "args": ["${workspaceFolder}/build.macos.sh"],
            "group": "build"
        },
        {
            "label": "Compile invaders (Win)",
            "type": "shell",
            "command": "${workspaceFolder}\\build.win.bat",
            "args": [],
            "group": "build"
        }
    ]
}
